---
title: Mapbox Setup
head:
  - tag: meta
    attrs:
      name: description
      content: Set up Mapbox as a map provider in MapConductor Android SDK — step-by-step integration guide.
---

import SecretsPluginRootGradleTabs from '~/components/setup/common/SecretsPluginRootGradleTabs.astro';
import AndroidManifestExample from '~/components/setup/common/AndroidManifestExample.astro';
import SecretsPropertiesExample from '~/components/setup/common/SecretsPropertiesExample.astro';
import GitignoreExample from '~/components/setup/common/GitignoreExample.astro';
import SettingsGradleTabs from '~/components/setup/mapbox/SettingsGradleTabs.astro';
import AppGradleTabs from '~/components/setup/mapbox/AppGradleTabs.astro';
import TestMapboxComposable from '~/components/setup/mapbox/TestMapboxComposable.astro';

This section covers the setup process for Mapbox Maps SDK integration with MapConductor.

:::tip
MapConductor provides a unified API layer on top of existing map SDKs. You must set up the Mapbox Maps SDK independently before using MapConductor's Mapbox integration.
:::

## Prerequisites

- Android development environment
- Mapbox account
- Mapbox access token

## Setup Steps

### 1. Mapbox Account Configuration

1. Sign up for a [Mapbox account](https://www.mapbox.com/)
2. Go to your [Mapbox Account Dashboard](https://account.mapbox.com/)
3. Navigate to **Access tokens**
4. Create a new access token or use the default public token
5. Copy the access token for use in your app

### 2. Gradle Configuration

Ensure the Mapbox Maven repository is configured (this repository already has it in `settings.gradle.kts`):

<SettingsGradleTabs />

Add the Secrets Gradle Plugin to your project's root `build.gradle.kts` or `build.gradle`:

<SecretsPluginRootGradleTabs />

Then, add the dependencies and apply the plugin in your app's `build.gradle.kts` or `build.gradle`:

<AppGradleTabs
  otherPluginsComment="... other plugins"
  mapboxSdkComment="Mapbox Maps SDK (NDK27 variant)"
  bomComment="MapConductor BOM for version management (v{BOM_MODULE_VERSION})"
  modulesComment="MapConductor modules (versions managed by BOM)"
/>

The Secrets Gradle Plugin automatically reads your `secrets.properties` file and injects the values into your `AndroidManifest.xml` at build time.

### 3. Android Manifest Configuration

Add the Mapbox access token placeholder to your `AndroidManifest.xml`:

<AndroidManifestExample
  metaDataEntries={[
    {name: "MAPBOX_ACCESS_TOKEN", value: "${MAPBOX_ACCESS_TOKEN}", comment: "Mapbox Access Token"}
  ]}
  permissions={[
    {name: "android.permission.INTERNET", comment: "Add internet and location permissions"},
    {name: "android.permission.ACCESS_COARSE_LOCATION"},
    {name: "android.permission.ACCESS_FINE_LOCATION"}
  ]}
/>

### 4. Access Token Configuration

Add your actual access token to your `secrets.properties` file:

<SecretsPropertiesExample
  entries={[
    {key: "MAPBOX_ACCESS_TOKEN", value: "your_actual_mapbox_access_token_here"}
  ]}
/>

**Important**:

- Never commit your `secrets.properties` file to version control. Add it to `.gitignore`.
- The Secrets Gradle Plugin automatically replaces `${MAPBOX_ACCESS_TOKEN}` in your `AndroidManifest.xml` with the actual value from this file.
- For CI/CD builds, you can set environment variables or use other secure methods to provide these values.

<GitignoreExample entries={["secrets.properties"]} />

### 5. Style Configuration

Mapbox allows custom map styles. You can use built-in styles or create custom ones, but this configuration is done in your Composable code rather than in the basic setup described here.

## Verification

To verify your Mapbox setup:

1. Build and run your app
2. Check that the map displays with Mapbox styling
3. Test map interactions (zoom, pan, rotate)

<TestMapboxComposable comment="If this displays correctly, your setup is working" />

## Troubleshooting

### Common Issues

**Map not displaying (blank screen)**

- Verify access token is correct in `secrets.properties`
- Check that the token has not expired
- Ensure internet connectivity for tile loading

**Access token errors**

- Double-check the token value (typically starts with `pk.`)
- Verify token permissions in Mapbox account dashboard
- Check token scopes include required permissions

**Build errors**

- Ensure the Mapbox SDK coordinates match your project configuration
- Verify the NDK27 variant is used
- Check that `secrets.properties` file exists and contains the token

## Next Steps

Once Mapbox Maps SDK is properly configured, you can use MapConductor's `MapboxMapView` component with the unified API.
