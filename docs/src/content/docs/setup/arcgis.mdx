---
title: ArcGIS Maps Setup
description: Set up ArcGIS as a map provider in MapConductor Android SDK — step-by-step integration guide.
---

import SecretsPluginRootGradleTabs from '~/components/setup/common/SecretsPluginRootGradleTabs.astro';
import AndroidManifestExample from '~/components/setup/common/AndroidManifestExample.astro';
import SecretsPropertiesExample from '~/components/setup/common/SecretsPropertiesExample.astro';
import GitignoreExample from '~/components/setup/common/GitignoreExample.astro';
import SettingsGradleTabs from '~/components/setup/arcgis/SettingsGradleTabs.astro';
import AppGradleTabs from '~/components/setup/arcgis/AppGradleTabs.astro';
import TestArcGISComposable from '~/components/setup/arcgis/TestArcGISComposable.astro';
import LicenseSetupExample from '~/components/setup/arcgis/LicenseSetupExample.astro';

This section describes the setup steps for integrating the ArcGIS Maps SDK with MapConductor.

:::tip
MapConductor provides a unified API layer on top of existing map SDKs. You must set up the ArcGIS Maps SDK independently before using MapConductor's ArcGIS integration.
:::

## Prerequisites

- Android development environment
- ArcGIS developer account
- ArcGIS API key

## Setup Steps

### 1. ArcGIS Developer Dashboard Configuration

1. Sign up for an [ArcGIS Developer account](https://developers.arcgis.com/)
2. Go to the [ArcGIS Developer Dashboard](https://developers.arcgis.com/dashboard)
3. Create a new application or select an existing one
4. Generate an API key with appropriate scopes
5. Note the API key for use in your app

### 2. Gradle Configuration

Ensure the Esri Maven repository is configured (this repository already includes it in `settings.gradle.kts`):

<SettingsGradleTabs />

Add the Secrets Gradle Plugin to your project's root `build.gradle.kts` or `build.gradle`:

<SecretsPluginRootGradleTabs />

Then, add the dependencies and apply the plugin in your app's `build.gradle.kts` or `build.gradle`:

<AppGradleTabs
  otherPluginsComment="... other plugins"
  arcgisSdkComment="ArcGIS Maps SDK"
  bomComment="MapConductor BOM for version management (v{BOM_MODULE_VERSION})"
  modulesComment="MapConductor modules (versions managed by BOM)"
/>

The Secrets Gradle Plugin automatically reads your `secrets.properties` file and can inject values into your `AndroidManifest.xml` at build time.

### 3. Android Manifest Configuration

Add the ArcGIS API key placeholder to your `AndroidManifest.xml`:

<AndroidManifestExample
  metaDataEntries={[
    {name: "ARCGIS_API_KEY", value: "${ARCGIS_API_KEY}", comment: "ArcGIS API Key"}
  ]}
  permissions={[
    {name: "android.permission.INTERNET", comment: "Add internet and location permissions"},
    {name: "android.permission.ACCESS_COARSE_LOCATION"},
    {name: "android.permission.ACCESS_FINE_LOCATION"}
  ]}
/>

### 4. API Key Configuration

Add your actual API key to your `secrets.properties` file. The Secrets Gradle Plugin automatically replaces `${ARCGIS_API_KEY}` in your `AndroidManifest.xml` with the actual value from this file.

<SecretsPropertiesExample
  entries={[
    {key: "ARCGIS_API_KEY", value: "your_actual_arcgis_api_key_here"}
  ]}
/>

:::caution
Never commit your `secrets.properties` file to version control. Add it to `.gitignore` to avoid accidental commits.
<GitignoreExample entries={["secrets.properties"]} />
:::

### 5. License Configuration

ArcGIS SDK requires proper license configuration. Configure licensing in your application:

<LicenseSetupExample />

## Verification

To verify your ArcGIS setup:

1. Build and run your app
2. Check that the ArcGIS map displays correctly
3. Test GIS-specific features (if using)
4. Verify authentication is working

<TestArcGISComposable comment="If this displays correctly, your setup is working" />

![ArcGIS setup verification](~/assets/setup/basic-setup-arcgis.png)

## Troubleshooting

### Common Issues

**Map not loading**

- Verify API key is correct in `secrets.properties`
- Check that the API key has proper scopes in ArcGIS Dashboard

**License errors**

- Ensure `ArcGISEnvironment.setApiKey()` is called before using the map
- For production apps, configure proper licensing according to Esri documentation

**Build errors**

- Ensure ArcGIS SDK coordinates match your project configuration
- Verify BOM is used for version management
- Check that Compose toolkit dependencies are included

## Next Steps

Once ArcGIS Maps SDK is properly configured, you can use MapConductor's `ArcGISMapView` component with the unified API.
