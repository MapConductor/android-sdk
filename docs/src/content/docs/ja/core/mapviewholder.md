---
title: "MapViewHolder"
---

`MapViewHolder` は、実際のネイティブ地図ビュー（`MapView` や `MapboxMapView` など）と Compose 層の間を橋渡しする役割を持つクラス/インターフェースです。

主な責務は次のとおりです。

- 実際のマップオブジェクトへの参照を保持する
- ライフサイクル管理（初期化・破棄）
- カメラ操作やオーバーレイ追加のための内部 API を提供

通常、アプリケーションコードから `MapViewHolder` を直接操作することはありませんが、拡張やデバッグ時に役立つ場合があります。

