---
title: Mapbox セットアップ
head:
  - tag: meta
    attrs:
      name: description
      content: MapConductor Android SDKでMapboxを使うためのステップバイステップ導入ガイド
---

import SecretsPluginRootGradleTabs from '~/components/setup/common/SecretsPluginRootGradleTabs.astro';
import AndroidManifestExample from '~/components/setup/common/AndroidManifestExample.astro';
import SecretsPropertiesExample from '~/components/setup/common/SecretsPropertiesExample.astro';
import GitignoreExample from '~/components/setup/common/GitignoreExample.astro';
import SettingsGradleTabs from '~/components/setup/mapbox/SettingsGradleTabs.astro';
import AppGradleTabs from '~/components/setup/mapbox/AppGradleTabs.astro';
import TestMapboxComposable from '~/components/setup/mapbox/TestMapboxComposable.astro';

このセクションでは、Mapbox Maps SDK と MapConductor を統合するためのセットアップ手順について説明します。

:::tip
MapConductor は既存の地図 SDK 上に統一された API レイヤーを提供します。MapConductor の Mapbox 統合を使用する前に、Mapbox Maps SDK を独立してセットアップする必要があります。
:::

## 前提条件

- Android 開発環境
- Mapbox アカウント
- Mapbox アクセストークン

## セットアップ手順

### 1. Mapbox アカウントの設定

1. [Mapbox アカウント](https://www.mapbox.com/)にサインアップします
2. [Mapbox Account Dashboard](https://account.mapbox.com/) にアクセスします
3. **Access tokens** に移動します
4. 新しいアクセストークンを作成するか、デフォルトの公開トークンを使用します
5. アプリで使用するためにアクセストークンをコピーします

### 2. Gradle の設定

Mapbox Maven リポジトリが設定されていることを確認します（このリポジトリでは `settings.gradle.kts` に既に含まれています）:

<SettingsGradleTabs />

プロジェクトのルート `build.gradle.kts` または `build.gradle` に Secrets Gradle Plugin を追加します:

<SecretsPluginRootGradleTabs />

次に、アプリの `build.gradle.kts` または `build.gradle` に依存関係を追加し、プラグインを適用します:

<AppGradleTabs
  otherPluginsComment="ほかのプラグイン"
  mapboxSdkComment="Mapbox Maps SDK（NDK27 バリアント）"
  bomComment="MapConductor BOM によるバージョン管理 (v{BOM_MODULE_VERSION})"
  modulesComment="MapConductor モジュール（BOM でバージョン管理）"
/>

Secrets Gradle Plugin は自動的に `secrets.properties` ファイルを読み取り、ビルド時に値を `AndroidManifest.xml` に注入します。

### 3. Android Manifest の設定

`AndroidManifest.xml` に Mapbox アクセストークンのプレースホルダーを追加します:

<AndroidManifestExample
  metaDataEntries={[
    {name: "MAPBOX_ACCESS_TOKEN", value: "${MAPBOX_ACCESS_TOKEN}", comment: "Mapbox Access Token"}
  ]}
  permissions={[
    {name: "android.permission.INTERNET", comment: "Add internet and location permissions"},
    {name: "android.permission.ACCESS_COARSE_LOCATION"},
    {name: "android.permission.ACCESS_FINE_LOCATION"}
  ]}
/>

### 4. アクセストークンの設定

`secrets.properties` ファイルに実際のアクセストークンを追加します。
- Secrets Gradle Plugin は、このファイルの実際の値で `AndroidManifest.xml` の `${MAPBOX_ACCESS_TOKEN}` を自動的に置き換えます。
- CI/CD ビルドの場合は、環境変数または他の安全な方法を使用してこれらの値を提供できます。

<SecretsPropertiesExample
  entries={[
    {key: "MAPBOX_ACCESS_TOKEN", value: "実際のアクセストークン"}
  ]}
/>

:::caution
`secrets.properties` ファイルをバージョン管理にコミットしないでください。
`.gitignore` に追加すると、不注意でコミットすることを避けることができます。
<GitignoreExample entries={["secrets.properties"]} />
:::

## 検証

Mapbox のセットアップを確認するには:

1. アプリをビルドして実行します
2. マップが Mapbox スタイルで表示されることを確認します
3. マップ操作（ズーム、パン、回転）をテストします

<TestMapboxComposable comment="もし地図が表示されれば、セットアップが正しくできています" />

![TestMapboxの実行結果](~/assets/setup/basic-setup-mapbox.png)

## トラブルシューティング

### よくある問題

**マップが表示されない（空白の画面）**

- `secrets.properties` のアクセストークンが正しいことを確認してください
- トークンの有効期限が切れていないことを確認してください
- タイルの読み込みのためのインターネット接続を確認してください

**アクセストークンエラー**

- トークンの値を再確認してください（通常は `pk.` で始まります）
- Mapbox アカウントダッシュボードでトークンのパーミッションを確認してください
- トークンスコープに必要なパーミッションが含まれていることを確認してください

**ビルドエラー**

- Mapbox SDK の座標がプロジェクト設定と一致していることを確認してください
- NDK27 バリアントが使用されていることを確認してください
- `secrets.properties` ファイルが存在し、トークンが含まれていることを確認してください

## 次のステップ

Mapbox Maps SDK が適切に設定されたら、統一 API を使用して MapConductor の `MapboxMapView` コンポーネントを使用できます。
