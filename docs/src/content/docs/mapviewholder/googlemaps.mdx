---
title: "GoogleMapViewHolder"
description: GoogleMapViewHolder — access the native Google Maps instance in MapConductor Android SDK.
---

import GoogleMapViewHolder from '~/components/core/mapviewholder/GoogleMapViewHolder.astro';
import GoogleMapViewHolderAccess from '~/components/core/mapviewholder/GoogleMapViewHolderAccess.astro';
import GoogleMapViewHolderExample from '~/components/core/mapviewholder/GoogleMapViewHolderExample.astro';


## Overview

To access Google Maps API-specific features, use the `GoogleMapViewHolder`.
The definition of `GoogleMapViewHolder` is as follows:

<GoogleMapViewHolder />
### Properties

- **`mapView: MapView`**: MapView generated inside the GoogleMapView component
- **`map: GoogleMap`**: GoogleMap class for manipulating the MapView

### Methods

- **`toScreenOffset(position: GeoPointInterface): Offset?`**: Returns the position on the map indicated by GeoPointInterface as a Dp value with the top-left of the MapView as (0,0)
- **`fromScreenOffset(offset: Offset): GeoPoint?`**: Returns the GeoPointInterface position on the map for the position specified by Dp value with the top-left of the MapView as (0,0)


## Getting GoogleMapViewHolder

`GoogleMapViewHolder` can be obtained through `getMapViewHolder()` after the map is initialized.

<GoogleMapViewHolderAccess />


### Sample

<GoogleMapViewHolderExample
  commentCameraPosition="Map camera position"
  commentRecompose="Using mutableStateOf, recompose when mapStyle changes"
  commentGetViewHolder="Get ViewHolder"
  commentNormalMap="Normal Google Maps"
  commentSimplifiedMap="Simplified map design"
/>

<video width="720" height="480" controls>
 <source src="/mapviewholder/map-view-holder-googlemaps-example.webm" type="video/webm" />
 <source src="/mapviewholder/map-view-holder-googlemaps-example.mp4" type="video/mp4" />
</video>
