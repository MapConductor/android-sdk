---
import { Code } from '@astrojs/starlight/components';

const code = `ImageIcon(
    drawable: Drawable,
    anchor: Offset = Offset(0.5f, 0.5f),
    debug: Boolean = false
)`;
---

<Code code={code} lang="kotlin" />

