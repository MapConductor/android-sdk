---
import { Code } from '@astrojs/starlight/components';

const code = `fun moveCameraTo(
    cameraPosition: MapCameraPositionImpl,
    durationMillis: Long? = 0,
)`;
---

<Code code={code} lang="kotlin" />

