---
import { Code } from '@astrojs/starlight/components';

interface Props {
  commentForPrint?: string;
}

const {
  commentForPrint = "Map loaded"
} = Astro.props;

const code = `MapView(
    ...
    onMapLoaded: {
        println("${commentForPrint}")
    },
) {
    ...
}`;
---

<Code code={code} lang="kotlin" />
