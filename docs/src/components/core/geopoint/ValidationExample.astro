---
import { Code } from '@astrojs/starlight/components';

interface Props {
  commentForUsage?: string;
  commentForUse?: string;
  latitude?: number;
  longitude?: number;
}

const {
  commentForUsage = "Usage example",
  commentForUse = "Use the point",
  latitude = 37.7749,
  longitude = -122.4194
} = Astro.props;

const code = `fun GeoPoint.isValid(): Boolean =
    latitude in -90.0..90.0 && longitude in -180.0..180.0

// ${commentForUsage}
val point = GeoPointImpl.fromLatLong(${latitude}, ${longitude})
if (point.isValid()) {
    // ${commentForUse}
}`;
---

<Code code={code} lang="kotlin" />
