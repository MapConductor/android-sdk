---
import { Code } from '@astrojs/starlight/components';

interface Props {
  southWestLatitude?: number;
  southWestLongitude?: number;
  northEastLatitude?: number;
  northEastLongitude?: number;
  pointLatitude?: number;
  pointLongitude?: number;
}

const {
  southWestLatitude = 20.0,
  southWestLongitude = 170.0,
  northEastLatitude = 40.0,
  northEastLongitude = -170.0,
  pointLatitude = 30.0,
  pointLongitude = 175.0
} = Astro.props;

const code = `// Bounds crossing the date line
val pacificBounds = GeoRectBounds(
    southWest = GeoPointImpl.fromLatLong(${southWestLatitude}, ${southWestLongitude}),  // West of date line
    northEast = GeoPointImpl.fromLatLong(${northEastLatitude}, ${northEastLongitude})  // East of date line
)

val pointInPacific = GeoPointImpl.fromLatLong(${pointLatitude}, ${pointLongitude})
val containsPoint = pacificBounds.contains(pointInPacific) // true`;
---

<Code code={code} lang="kotlin" />

