---
import { Code } from '@astrojs/starlight/components';

const code = `interface MarkerAnimation {
    val duration: Long
    val interpolator: TimeInterpolator?

    // Animation lifecycle methods
    fun onStart()
    fun onUpdate(progress: Float)
    fun onEnd()
}`;
---

<Code code={code} lang="kotlin" />

