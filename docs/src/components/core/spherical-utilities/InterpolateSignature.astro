---
import { Code } from '@astrojs/starlight/components';

const code = `fun interpolate(from: GeoPointInterface, to: GeoPointInterface, fraction: Double): GeoPoint`;
---

<Code code={code} lang="kotlin" />

