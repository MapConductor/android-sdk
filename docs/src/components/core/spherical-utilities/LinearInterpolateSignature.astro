---
import { Code } from '@astrojs/starlight/components';

const code = `Planar.interpolate(from: GeoPointInterface, to: GeoPointInterface, fraction: Double): GeoPoint`;
---

<Code code={code} lang="kotlin" />

