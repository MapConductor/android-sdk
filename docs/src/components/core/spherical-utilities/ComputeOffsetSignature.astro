---
import { Code } from '@astrojs/starlight/components';

const code = `fun computeOffset(origin: GeoPoint, distance: Double, heading: Double): GeoPointImpl`;
---

<Code code={code} lang="kotlin" />

