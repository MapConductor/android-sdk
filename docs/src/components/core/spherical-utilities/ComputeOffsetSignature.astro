---
import { Code } from '@astrojs/starlight/components';

const code = `fun computeOffset(origin: GeoPointInterface, distance: Double, heading: Double): GeoPoint`;
---

<Code code={code} lang="kotlin" />

