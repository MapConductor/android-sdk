---
import { Code } from '@astrojs/starlight/components';

const code = `interface MapCameraPosition {
    val position: GeoPoint
    val zoom: Double
    val bearing: Double
    val tilt: Double
    val paddings: MapPaddings?
    val visibleRegion: VisibleRegion?
}`;
---

<Code code={code} lang="kotlin" />

