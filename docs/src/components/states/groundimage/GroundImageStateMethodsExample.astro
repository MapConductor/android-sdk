---
import { Code } from '@astrojs/starlight/components';

interface Props {
  commentForFingerPrint?: string;
  commentForAsFlow?: string;
}

const {
  commentForFingerPrint = "変更検知",
  commentForAsFlow = "リアクティブ更新"
} = Astro.props;

const code = `fun fingerPrint(): GroundImageFingerPrint // ${commentForFingerPrint}
fun asFlow(): Flow<GroundImageFingerPrint> // ${commentForAsFlow}`;
---

<Code code={code} lang="kotlin" />
